rails s -b $IP -p $PORT
rails server -b $IP -p $PORT
cd saasapp/
rails server -b $IP -p $PORT
git status
git add .
git commit -m "updated navbar styles"
git push origin pages_and_layout
rails server -b $IP -p $PORT
rails s -b $IP -p $PORT
git status
git add .
git commit -m "refactored with sass"
git push origin pages_and_layout
clear
cd saasapp/
bundle install
bundle update
clear
git status 
git add . 
git commit - "added font awesome gem"
git commit -m "added font awesome gem"
git push origin pages_and_layout
clear
rails routs
rails routes
rails s _b $IP -p $PORT
rails s -b $IP -p $PORT
rails routes
git stash
git checkout pages_and_layout
git reset --hard origin origin/master
git reset -- hard origin/master
cd
clear
git stash
git reset --hard origin/master
$ git clone https://github.com/schacon/simplegit-progit
git log
git log -p
cd saasapp
rails s -b $IP -p $PORT
git checkout -- <bad filename>
git reset --hard 473e29f
git reset --hard 260fbbe
sudo git rese --hard 260fbbe
reset
git reset --hard 260fbbe
$ sudo chmod -R g+w
sudo chmod -R g+w
git clone git@github.com:bolcese17/upskill_saas_tutorial.git
cd upskill/saas/tutorial
cd
cd upskill/saas/tutorial
git reset --hard HEAD^
git reset --hard
sudo  git reset --hard
sudo git reset --hard
chrootkit
chkrootkit
git reset --hard
sudo git checkout -- sites/default
git clean --force -d
git reset --hard
cat ~/.ssh/id_rsa.pub
r -rm saasapp
rmdir- saasapp /s
rm
rm --help
rm ./saasapp
rmdir ./saasapp
rm -r ./saasapp
git reset --hard
lsof +D .ssh/authorized_keys
df -h
lsof +D /path
lsof +D /saasapp
lsof +D /workspace
lsof +D /
mkdir -p ~/.ssh/
cat ~/id_rsa.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
cat ~/id_rsa.pub >> ~/.ssh/authorized_keys
ls ~/.ssh/id_rsa*
Sounds like you're almost there! I'm not sure exactly what you have and haven't done though, so I'll explain the whole process.
First, I'm guessing (because you're using puTTY) that your computer runs Windows? If so, first you'll need to install Git for Windows, which you can download from the official Git website. Download it and install it, accepting the default choices in the installer.
That will leave you with an item in your Start menu called Git Bash. You'll use this to perform what comes next. (You don't actually need Git itself installed, but the Git for Windows installer adds some additional tools like ssh-keygen that you will need.)
If your computer is actually running Linux or Mac OS X rather than Windows then you already have the tools you need. You can follow the same instructions, but instead of using Git Bash to enter commands, use a terminal window.
From now on, I'll just refer to typing things "in the terminal". If you're using Windows, type these things in the Git Bash window.
Step 1: On your own computer, check for an SSH key pair
In the terminal, type:
ls ~/.ssh/id_rsa*
This should list two files: id_rsa and id_rsa.pub. If they exist, move on to step 2. If not, type:
ssh-keygen
scp ~/.ssh/id_rsa.pub git@my-server.com
ssh git@my-server.com
mkdir -p ~/.ssh/
cat ~/id_rsa.pub >> ~/.ssh/authorized_keys
rm ~/.ssh/id_rsa.pub
chmod 700 ~/.ssh
chmod 400 ~/.ssh/authorized_keys
git git checkout -- <bad filename>
git checkout -- <bad filename>
git checkout -- 
git reset <473e29f488c7db9dbd492a818ca11b7abe0ea57c>
git reset 473e29f488c7db9dbd492a818ca11b7abe0ea57c
git rm .gitattributes
git reset --hard
rails s -b $IP -p $PORT
cd saasapp/
rails s -b $IP -p $PORT
git clean -df
rails s -b $IP -p $PORT
clear
rails s -b $IP -p $PORT
clear
git status
clear
rails s -b $IP -p %PORT
rails s -b $IP -p $PORT
git status
git add .
git commit -m "Updated nav links using link_to"
git push origin pages_and_layout
clear
rails routs 
clear
rails routes
git status 
git add .
git commi -m "updated links to use prefixes"
git commit -m "updated links to use prefixes"
git push origin pages_and_layout
rails s -b $IP -p $PORT
clear
rails s-b $Ip -p $PORT
rails s -b $IP -p $PORT
